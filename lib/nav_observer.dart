import 'package:flutter/widgets.dart';

/// Global route observer used to detect when pages are popped back to
final RouteObserver<ModalRoute<void>> routeObserver =
    RouteObserver<ModalRoute<void>>();
